/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['darmaaka.com'],
    // minimumCacheTTL: 60,
    // unoptimized: true,
    // loader: 'custom',
    // loaderFile: './my-loader.ts',
  },
  // output: 'export',
  // trailingSlash: true,
};

module.exports = nextConfig;
